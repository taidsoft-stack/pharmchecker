<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>로그인 - PharmChecker</title>
  <script src="https://accounts.google.com/gsi/client" async defer></script>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    body {
      font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      min-height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      padding: 20px;
    }
    .login-container {
      background: white;
      border-radius: 20px;
      box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
      max-width: 450px;
      width: 100%;
      overflow: hidden;
    }
    .login-header {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: white;
      padding: 40px 30px;
      text-align: center;
    }
    .login-header h1 {
      font-size: 32px;
      margin-bottom: 10px;
      font-weight: 700;
    }
    .login-header p {
      font-size: 16px;
      opacity: 0.9;
    }
    .logo {
      width: 80px;
      height: 80px;
      background: rgba(255, 255, 255, 0.2);
      border-radius: 50%;
      margin: 0 auto 20px;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 40px;
    }
    .login-content {
      padding: 50px 40px;
      text-align: center;
    }
    .login-content h2 {
      font-size: 24px;
      color: #2c3e50;
      margin-bottom: 15px;
    }
    .login-content p {
      color: #7f8c8d;
      margin-bottom: 40px;
      line-height: 1.6;
    }
    .google-btn-container {
      display: flex;
      justify-content: center;
      margin-bottom: 30px;
    }
    .divider {
      display: flex;
      align-items: center;
      margin: 30px 0;
      color: #bdc3c7;
      font-size: 14px;
    }
    .divider::before,
    .divider::after {
      content: '';
      flex: 1;
      border-bottom: 1px solid #ecf0f1;
    }
    .divider span {
      padding: 0 10px;
    }
    .features {
      text-align: left;
      margin-top: 30px;
    }
    .feature-item {
      display: flex;
      align-items: center;
      margin-bottom: 15px;
      color: #34495e;
      font-size: 14px;
    }
    .feature-item::before {
      content: '✓';
      display: inline-block;
      width: 24px;
      height: 24px;
      background: #27ae60;
      color: white;
      border-radius: 50%;
      text-align: center;
      line-height: 24px;
      margin-right: 12px;
      font-weight: bold;
    }
    .footer-text {
      text-align: center;
      color: #95a5a6;
      font-size: 12px;
      margin-top: 30px;
      padding-top: 20px;
      border-top: 1px solid #ecf0f1;
    }
    
    /* 로딩 스피너 */
    .spinner {
      display: none;
      border: 4px solid #f3f3f3;
      border-top: 4px solid #667eea;
      border-radius: 50%;
      width: 40px;
      height: 40px;
      animation: spin 1s linear infinite;
      margin: 20px auto;
    }
    @keyframes spin {
      0% { transform: rotate(0deg); }
      100% { transform: rotate(360deg); }
    }
    
    /* 실패 팝업 */
    .error-popup {
      display: none;
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: rgba(0, 0, 0, 0.5);
      z-index: 1000;
      justify-content: center;
      align-items: center;
    }
    .error-popup.active {
      display: flex;
    }
    .error-content {
      background: white;
      padding: 40px;
      border-radius: 16px;
      text-align: center;
      max-width: 400px;
      box-shadow: 0 10px 40px rgba(0, 0, 0, 0.3);
    }
    .error-icon {
      width: 60px;
      height: 60px;
      background: #e74c3c;
      color: white;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 30px;
      margin: 0 auto 20px;
    }
    .error-content h3 {
      color: #2c3e50;
      margin-bottom: 10px;
      font-size: 20px;
    }
    .error-content p {
      color: #7f8c8d;
      margin-bottom: 30px;
    }
    .close-btn {
      background: #e74c3c;
      color: white;
      border: none;
      padding: 12px 40px;
      border-radius: 8px;
      font-size: 16px;
      font-weight: 600;
      cursor: pointer;
      transition: all 0.3s ease;
    }
    .close-btn:hover {
      background: #c0392b;
      transform: translateY(-2px);
      box-shadow: 0 4px 12px rgba(231, 76, 60, 0.4);
    }
    
    .signup-link {
      margin-top: 20px;
      padding-top: 20px;
      border-top: 1px solid #ecf0f1;
      text-align: center;
      color: #7f8c8d;
      font-size: 14px;
    }
    
    .signup-link a {
      color: #667eea;
      text-decoration: none;
      font-weight: 600;
      transition: color 0.2s;
    }
    
    .signup-link a:hover {
      color: #5568d3;
      text-decoration: underline;
    }
  </style>
</head>
<body>
  <div class="login-container">
    <div class="login-header">
      <div class="logo">💊</div>
      <h1>PharmChecker</h1>
      <p>의료 B2B 통합 솔루션</p>
    </div>
    
    <div class="login-content">
      <h2>로그인</h2>
      <p>구글 계정으로 간편하게 시작하세요</p>
      
      <div class="google-btn-container">
        <div id="g_id_onload"
             data-client_id="506078799522-9ul40knlju9485bp654m76l2c0u76cbf.apps.googleusercontent.com"
             data-callback="handleCredentialResponse"
             data-auto_prompt="false">
        </div>
        <div class="g_id_signin"
             data-type="standard"
             data-size="large"
             data-theme="outline"
             data-text="sign_in_with"
             data-shape="rectangular"
             data-logo_alignment="left">
        </div>
      </div>
      
      <div class="spinner" id="loading"></div>
      
      <div class="divider">
        <span>PharmChecker의 주요 기능</span>
      </div>
      
      <div class="features">
        <div class="feature-item">실시간 복약 순응도 모니터링</div>
        <div class="feature-item">스마트 알림 시스템</div>
        <div class="feature-item">환자별 맞춤 관리</div>
        <div class="feature-item">데이터 기반 인사이트 제공</div>
      </div>
      
      <div class="signup-link">
        계정이 없으신가요? <a href="/join">회원가입</a>
      </div>
      
      <div class="footer-text">
        © 2025 PharmChecker. All rights reserved.
      </div>
    </div>
  </div>

  <!-- 로그인 실패 팝업 -->
  <div class="error-popup" id="errorPopup">
    <div class="error-content">
      <div class="error-icon">✕</div>
      <h3>로그인 실패</h3>
      <p>로그인에 실패했습니다.<br>다시 시도해주세요.</p>
      <button class="close-btn" onclick="closeErrorPopup()">닫기</button>
    </div>
  </div>

  <script>
    // 페이지 로드 시 세션 체크
    window.addEventListener('load', function() {
      const user = JSON.parse(sessionStorage.getItem('user') || '{}');
      if (user.userId) {
        // 이미 로그인되어 있으면 pharmchecker로 리다이렉트
        window.location.href = '/pharmchecker';
        return;
      }
    });

    // 구글 로그인 응답 처리
    async function handleCredentialResponse(response) {
      console.log("Encoded JWT ID token: " + response.credential);
      
      // 로딩 표시
      document.getElementById('loading').style.display = 'block';
      
      // JWT 토큰 디코딩 (간단한 파싱)
      try {
        const payload = parseJwt(response.credential);
        console.log("User info:", payload);
        
        // 서버에 로그인 요청
        const loginResponse = await fetch('/api/login', {
          method: 'POST',
          headers: {
            'Content-Type': 'application/json',
          },
          body: JSON.stringify({
            email: payload.email
          })
        });

        const result = await loginResponse.json();

        if (loginResponse.ok && result.success) {
          // 로그인 성공 - 세션에 사용자 정보 저장
          sessionStorage.setItem('user', JSON.stringify({
            userId: result.data.userId,
            email: result.data.email,
            name: result.data.pharmacistName,
            pharmacyName: result.data.pharmacyName,
            picture: result.data.googlePicture || payload.picture
          }));
          
          // pharmchecker 페이지로 이동
          setTimeout(() => {
            window.location.href = '/pharmchecker';
          }, 500);
        } else {
          // 로그인 실패 - 등록되지 않은 이메일
          document.getElementById('loading').style.display = 'none';
          alert(result.message || '로그인에 실패했습니다.');
          
          // 회원가입 페이지로 이동할지 확인
          if (confirm('등록되지 않은 계정입니다. 회원가입 페이지로 이동하시겠습니까?')) {
            window.location.href = '/join';
          }
        }
        
      } catch (error) {
        console.error("Login failed:", error);
        document.getElementById('loading').style.display = 'none';
        showErrorPopup();
      }
    }
    
    // JWT 토큰 파싱 함수
    function parseJwt(token) {
      const base64Url = token.split('.')[1];
      const base64 = base64Url.replace(/-/g, '+').replace(/_/g, '/');
      const jsonPayload = decodeURIComponent(atob(base64).split('').map(function(c) {
        return '%' + ('00' + c.charCodeAt(0).toString(16)).slice(-2);
      }).join(''));
      return JSON.parse(jsonPayload);
    }
    
    // 에러 팝업 표시
    function showErrorPopup() {
      document.getElementById('errorPopup').classList.add('active');
    }
    
    // 에러 팝업 닫기 및 페이지 새로고침
    function closeErrorPopup() {
      document.getElementById('errorPopup').classList.remove('active');
      window.location.reload();
    }
    
    // 개발 모드: 구글 클라이언트 ID가 없을 때 임시 로그인 버튼 표시
    window.addEventListener('load', function() {
      // 구글 클라이언트 ID를 설정하지 않은 경우를 위한 개발용 처리
      const clientId = document.querySelector('[data-client_id]').getAttribute('data-client_id');
      if (clientId === 'YOUR_GOOGLE_CLIENT_ID') {
        console.warn('⚠️ 구글 클라이언트 ID를 설정해주세요.');
        // 개발 모드: 임시 로그인 버튼 생성
        const devButton = document.createElement('button');
        devButton.textContent = '개발 모드 로그인 (테스트용)';
        devButton.style.cssText = 'padding: 12px 24px; background: #4285f4; color: white; border: none; border-radius: 8px; cursor: pointer; font-size: 14px; margin-top: 20px;';
        devButton.onclick = function() {
          // 테스트용 JWT 생성 및 로그인
          handleCredentialResponse({
            credential: createTestJWT()
          });
        };
        document.querySelector('.google-btn-container').appendChild(devButton);
      }
    });
    
    // 테스트용 JWT 생성
    function createTestJWT() {
      const header = btoa(JSON.stringify({alg: "HS256", typ: "JWT"}));
      const payload = btoa(JSON.stringify({
        email: 'test@pharmchecker.com',
        name: '테스트 사용자',
        picture: ''
      }));
      return header + '.' + payload + '.signature';
    }
  </script>
</body>
</html>
