<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>회원가입 - PharmChecker</title>
  <script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>
  <script src="//t1.daumcdn.net/mapjsapi/bundle/postcode/prod/postcode.v2.js"></script>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    body {
      font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      min-height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      padding: 40px 20px;
    }
    .signup-container {
      background: white;
      border-radius: 20px;
      box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
      max-width: 600px;
      width: 100%;
      overflow: hidden;
    }
    .signup-header {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: white;
      padding: 40px 30px;
      text-align: center;
    }
    .signup-header h1 {
      font-size: 32px;
      margin-bottom: 10px;
      font-weight: 700;
    }
    .signup-header p {
      font-size: 16px;
      opacity: 0.9;
    }
    .logo {
      width: 80px;
      height: 80px;
      background: rgba(255, 255, 255, 0.2);
      border-radius: 50%;
      margin: 0 auto 20px;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 40px;
    }
    .signup-content {
      padding: 40px;
    }
    .step-indicator {
      text-align: center;
      margin-bottom: 30px;
      color: #7f8c8d;
      font-size: 14px;
    }
    .step {
      display: inline-block;
      margin: 0 5px;
      width: 30px;
      height: 30px;
      border-radius: 50%;
      background: #ecf0f1;
      line-height: 30px;
      font-weight: 600;
    }
    .step.active {
      background: #667eea;
      color: white;
    }
    .step.completed {
      background: #27ae60;
      color: white;
    }
    .google-auth-section {
      text-align: center;
      padding: 30px 0;
    }
    .google-auth-section h2 {
      font-size: 20px;
      color: #2c3e50;
      margin-bottom: 15px;
    }
    .google-auth-section p {
      color: #7f8c8d;
      margin-bottom: 20px;
      font-size: 14px;
    }
    .google-btn-container {
      display: flex;
      justify-content: center;
      margin-bottom: 20px;
    }
    .auth-success {
      display: none;
      background: #d4edda;
      border: 1px solid #c3e6cb;
      color: #155724;
      padding: 12px 20px;
      border-radius: 8px;
      margin-top: 20px;
      font-size: 14px;
    }
    .auth-success.show {
      display: block;
    }
    .form-section {
      display: none;
      margin-top: 30px;
    }
    .form-section.show {
      display: block;
    }
    .form-group {
      margin-bottom: 20px;
    }
    .form-group label {
      display: block;
      color: #2c3e50;
      font-weight: 600;
      margin-bottom: 8px;
      font-size: 14px;
    }
    .form-group label .required {
      color: #e74c3c;
      margin-left: 4px;
    }
    .form-group input {
      width: 100%;
      padding: 12px 16px;
      border: 2px solid #e5e8eb;
      border-radius: 8px;
      font-size: 14px;
      transition: border-color 0.2s;
    }
    .form-group input:focus {
      outline: none;
      border-color: #667eea;
    }
    .form-group input.error {
      border-color: #e74c3c;
      animation: shake 0.3s;
    }
    @keyframes shake {
      0%, 100% { transform: translateX(0); }
      25% { transform: translateX(-5px); }
      75% { transform: translateX(5px); }
    }
    .form-group input[readonly] {
      background-color: #f8f9fa;
      cursor: not-allowed;
    }
    .form-group .helper-text {
      font-size: 12px;
      color: #7f8c8d;
      margin-top: 5px;
    }
    .address-search-btn {
      width: 100%;
      padding: 12px;
      background: #667eea;
      color: white;
      border: none;
      border-radius: 8px;
      font-size: 14px;
      font-weight: 600;
      cursor: pointer;
      transition: all 0.2s;
      margin-bottom: 10px;
    }
    .address-search-btn:hover {
      background: #5568d3;
    }
    .address-inputs {
      display: grid;
      gap: 10px;
    }
    .submit-btn {
      width: 100%;
      padding: 16px;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: white;
      border: none;
      border-radius: 8px;
      font-size: 16px;
      font-weight: 600;
      cursor: pointer;
      transition: all 0.3s;
      margin-top: 20px;
    }
    .submit-btn:hover {
      transform: translateY(-2px);
      box-shadow: 0 4px 12px rgba(102, 126, 234, 0.4);
    }
    .submit-btn:disabled {
      background: #bdc3c7;
      cursor: not-allowed;
      transform: none;
    }
    .login-link {
      text-align: center;
      margin-top: 20px;
      padding-top: 20px;
      border-top: 1px solid #ecf0f1;
      color: #7f8c8d;
      font-size: 14px;
    }
    .login-link a {
      color: #667eea;
      text-decoration: none;
      font-weight: 600;
    }
    .login-link a:hover {
      text-decoration: underline;
    }
  </style>
</head>
<body>
  <div class="signup-container">
    <div class="signup-header">
      <div class="logo">💊</div>
      <h1>PharmChecker</h1>
      <p>회원가입</p>
    </div>
    
    <div class="signup-content">
      <div class="step-indicator">
        <span class="step active" id="step1">1</span>
        <span class="step" id="step2">2</span>
      </div>
      
      <!-- Step 1: 구글 계정 인증 -->
      <div class="google-auth-section" id="authSection">
        <h2>구글 계정으로 인증</h2>
        <p>회원가입을 위해 구글 계정 인증이 필요합니다</p>
        
        <div class="google-btn-container">
          <button class="google-signin-btn" onclick="signInWithGoogle()">
            <svg class="google-icon" style="width: 20px; height: 20px; margin-right: 12px;" viewBox="0 0 24 24">
              <path fill="#4285F4" d="M22.56 12.25c0-.78-.07-1.53-.2-2.25H12v4.26h5.92c-.26 1.37-1.04 2.53-2.21 3.31v2.77h3.57c2.08-1.92 3.28-4.74 3.28-8.09z"/>
              <path fill="#34A853" d="M12 23c2.97 0 5.46-.98 7.28-2.66l-3.57-2.77c-.98.66-2.23 1.06-3.71 1.06-2.86 0-5.29-1.93-6.16-4.53H2.18v2.84C3.99 20.53 7.7 23 12 23z"/>
              <path fill="#FBBC05" d="M5.84 14.09c-.22-.66-.35-1.36-.35-2.09s.13-1.43.35-2.09V7.07H2.18C1.43 8.55 1 10.22 1 12s.43 3.45 1.18 4.93l2.85-2.22.81-.62z"/>
              <path fill="#EA4335" d="M12 5.38c1.62 0 3.06.56 4.21 1.64l3.15-3.15C17.45 2.09 14.97 1 12 1 7.7 1 3.99 3.47 2.18 7.07l3.66 2.84c.87-2.6 3.3-4.53 6.16-4.53z"/>
            </svg>
            Google로 계속하기
          </button>
        </div>
        
        <div class="auth-success" id="authSuccess">
          ✓ 구글 계정 인증이 완료되었습니다!
        </div>
      </div>
      
      <!-- Step 2: 약국 정보 입력 -->
      <div class="form-section" id="formSection">
        <h2 style="font-size: 20px; color: #2c3e50; margin-bottom: 20px;">약국 정보 입력</h2>
        
        <form id="signupForm" onsubmit="handleSubmit(event)">
          <div class="form-group">
            <label for="email">이메일 주소<span class="required">*</span></label>
            <input type="email" id="email" name="email" readonly required>
            <div class="helper-text">구글 계정 이메일이 자동으로 입력됩니다</div>
          </div>
          
          <div class="form-group">
            <label for="pharmacistName">대표 약사명<span class="required">*</span></label>
            <input type="text" id="pharmacistName" name="pharmacistName" placeholder="홍길동" required>
          </div>
          
          <div class="form-group">
            <label for="pharmacistPhone">핸드폰 번호<span class="required">*</span></label>
            <input type="tel" id="pharmacistPhone" name="pharmacistPhone" placeholder="010-1234-5678" maxlength="13" required>
            <div class="helper-text">'-' 포함하여 입력해주세요 (예: 010-1234-5678)</div>
          </div>
          
          <div class="form-group">
            <label for="businessNumber">사업자 번호<span class="required">*</span></label>
            <input type="text" id="businessNumber" name="businessNumber" placeholder="123-45-67890" maxlength="12" required>
            <div class="helper-text">'-' 포함하여 입력해주세요 (예: 123-45-67890)</div>
          </div>
          
          <div class="form-group">
            <label for="pharmacyName">약국 이름<span class="required">*</span></label>
            <input type="text" id="pharmacyName" name="pharmacyName" placeholder="행복약국" required>
          </div>
          
          <div class="form-group">
            <label for="pharmacyPhone">약국 대표번호<span class="required">*</span></label>
            <input type="tel" id="pharmacyPhone" name="pharmacyPhone" placeholder="02-1234-5678" maxlength="13" required>
            <div class="helper-text">'-' 포함하여 입력해주세요</div>
          </div>
          
          <div class="form-group">
            <label for="pharmacyAddress">약국 주소<span class="required">*</span></label>
            <button type="button" class="address-search-btn" onclick="searchAddress()">📍 주소 검색</button>
            <div class="address-inputs">
              <input type="text" id="postcode" name="postcode" placeholder="우편번호" readonly required>
              <input type="text" id="address" name="address" placeholder="도로명 주소 또는 지번 주소" readonly required>
              <input type="text" id="detailAddress" name="detailAddress" placeholder="상세주소 (선택)">
            </div>
          </div>
          
          <div class="form-group">
            <label for="referralCode">추천인 코드 (선택)</label>
            <input type="text" id="referralCode" name="referralCode" placeholder="영업담당자에게 받은 코드 입력" maxlength="20" style="text-transform: uppercase;">
            <div class="helper-text">추천인 코드 입력 시 1개월 무료 혜택이 제공됩니다</div>
          </div>
          
          <button type="submit" class="submit-btn" id="submitBtn" disabled>회원가입 완료</button>
        </form>
        
        <div class="login-link">
          이미 계정이 있으신가요? <a href="/login">로그인</a>
        </div>
      </div>
      
      <!-- 로그인 페이지 링크 (인증 전) -->
      <div class="login-link" id="loginLinkAuth">
        이미 계정이 있으신가요? <a href="/login">로그인</a>
      </div>
    </div>
  </div>

  <script>
    // Supabase 클라이언트 초기화
    const supabaseClient = window.supabase.createClient(
      'https://gitbtujexmsjfixgeoha.supabase.co',
      'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImdpdGJ0dWpleG1zamZpeGdlb2hhIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjY0NzA5MDIsImV4cCI6MjA4MjA0NjkwMn0.BNN8hauH8NdHZ4vopW_CQ_iK9CR55nfp3JQwuTjrG48'
    );

    let authUserId = '';
    let userEmail = '';
    let userName = '';
    let userPicture = '';
    let accessToken = '';
    
    // Google 로그인
    async function signInWithGoogle() {
      try {
        const { data, error } = await supabaseClient.auth.signInWithOAuth({
          provider: 'google',
          options: {
            redirectTo: `${window.location.origin}/join?auth=callback`
          }
        });
        
        if (error) {
          console.error('인증 실패:', error);
          alert('인증에 실패했습니다: ' + error.message);
        }
      } catch (error) {
        console.error('인증 오류:', error);
        alert('인증 중 오류가 발생했습니다.');
      }
    }

    // 페이지 로드 시 callback 처리
    window.addEventListener('DOMContentLoaded', async function() {
      const urlParams = new URLSearchParams(window.location.search);
      
      if (urlParams.get('auth') === 'callback') {
        const { data: { session }, error } = await supabaseClient.auth.getSession();
        
        if (error || !session) {
          alert('인증에 실패했습니다. 다시 시도해주세요.');
          return;
        }

        const user = session.user;
        authUserId = user.id;
        userEmail = user.email;
        userName = user.user_metadata.full_name || user.email.split('@')[0];
        userPicture = user.user_metadata.avatar_url || '';
        accessToken = session.access_token;

        console.log('User info:', user);
        
        // 이미 가입된 사용자인지 확인
        try {
          const checkResponse = await fetch('/api/auth/check-existing-user', {
            method: 'POST',
            headers: { 
              'Content-Type': 'application/json',
              'Authorization': `Bearer ${accessToken}`
            },
            body: JSON.stringify({ userId: authUserId })
          });

          const checkData = await checkResponse.json();

          if (checkData.isExistingUser) {
            // 이미 가입된 계정
            alert('이미 가입하신 계정입니다.\n로그인 페이지로 이동합니다.');
            window.location.href = '/login';
            return;
          }
          
          // 신규 회원 - 인증 성공 표시
          document.getElementById('authSuccess').classList.add('show');
          document.getElementById('step1').classList.add('completed');
          document.getElementById('step2').classList.add('active');
          
          // 폼 섹션 표시
          setTimeout(() => {
            document.getElementById('authSection').style.display = 'none';
            document.getElementById('loginLinkAuth').style.display = 'none';
            document.getElementById('formSection').classList.add('show');
            
            // 이메일 자동 입력
            document.getElementById('email').value = userEmail;
            
            // 제출 버튼 활성화
            document.getElementById('submitBtn').disabled = false;
          }, 1000);
          
        } catch (error) {
          console.error("User check failed:", error);
          alert('사용자 확인에 실패했습니다. 다시 시도해주세요.');
        }
      }
    });
    
    // JWT 토큰 파싱 (필요시 사용)
    function parseJwt(token) {
      const base64Url = token.split('.')[1];
      const base64 = base64Url.replace(/-/g, '+').replace(/_/g, '/');
      const jsonPayload = decodeURIComponent(atob(base64).split('').map(function(c) {
        return '%' + ('00' + c.charCodeAt(0).toString(16)).slice(-2);
      }).join(''));
      return JSON.parse(jsonPayload);
    }
    
    // 다음 카카오 주소 검색 API
    function searchAddress() {
      new daum.Postcode({
        oncomplete: function(data) {
          // 도로명 주소와 지번 주소 중 선택한 것 사용
          let addr = '';
          
          if (data.userSelectedType === 'R') {
            addr = data.roadAddress; // 도로명 주소
          } else {
            addr = data.jibunAddress; // 지번 주소
          }
          
          // 우편번호와 주소 정보를 필드에 입력
          document.getElementById('postcode').value = data.zonecode;
          document.getElementById('address').value = addr;
          
          // 상세주소 입력란으로 포커스 이동
          document.getElementById('detailAddress').focus();
        }
      }).open();
    }
    
    // 핸드폰 번호 자동 하이픈 추가 및 유효성 검증
    function formatPhoneNumber(input) {
      let value = input.value.replace(/[^0-9]/g, ''); // 숫자만 추출
      let formatted = '';
      
      if (value.length <= 3) {
        formatted = value;
      } else if (value.length <= 7) {
        formatted = value.substr(0, 3) + '-' + value.substr(3);
      } else if (value.length <= 11) {
        formatted = value.substr(0, 3) + '-' + value.substr(3, 4) + '-' + value.substr(7);
      } else {
        formatted = value.substr(0, 3) + '-' + value.substr(3, 4) + '-' + value.substr(7, 4);
      }
      
      input.value = formatted;
      
      // 유효성 검증
      const phonePattern = /^01[0-9]-\d{3,4}-\d{4}$/;
      if (formatted.length >= 12 && !phonePattern.test(formatted)) {
        input.setCustomValidity('올바른 핸드폰 번호 형식이 아닙니다 (010-1234-5678)');
      } else {
        input.setCustomValidity('');
      }
    }
    
    // 일반 전화번호 자동 하이픈 추가
    function formatTelNumber(input) {
      let value = input.value.replace(/[^0-9]/g, '');
      let formatted = '';
      
      // 서울 02
      if (value.startsWith('02')) {
        if (value.length <= 2) {
          formatted = value;
        } else if (value.length <= 5) {
          formatted = value.substr(0, 2) + '-' + value.substr(2);
        } else if (value.length <= 9) {
          formatted = value.substr(0, 2) + '-' + value.substr(2, 3) + '-' + value.substr(5);
        } else {
          formatted = value.substr(0, 2) + '-' + value.substr(2, 4) + '-' + value.substr(6, 4);
        }
      } 
      // 지역번호 3자리 (031, 032 등)
      else {
        if (value.length <= 3) {
          formatted = value;
        } else if (value.length <= 6) {
          formatted = value.substr(0, 3) + '-' + value.substr(3);
        } else if (value.length <= 10) {
          formatted = value.substr(0, 3) + '-' + value.substr(3, 3) + '-' + value.substr(6);
        } else {
          formatted = value.substr(0, 3) + '-' + value.substr(3, 4) + '-' + value.substr(7, 4);
        }
      }
      
      input.value = formatted;
    }
    
    // 사업자 번호 자동 하이픈 추가
    function formatBusinessNumber(input) {
      let value = input.value.replace(/[^0-9]/g, '');
      let formatted = '';
      
      if (value.length <= 3) {
        formatted = value;
      } else if (value.length <= 5) {
        formatted = value.substr(0, 3) + '-' + value.substr(3);
      } else if (value.length <= 10) {
        formatted = value.substr(0, 3) + '-' + value.substr(3, 2) + '-' + value.substr(5);
      } else {
        formatted = value.substr(0, 3) + '-' + value.substr(3, 2) + '-' + value.substr(5, 5);
      }
      
      input.value = formatted;
      
      // 10자리 완성 여부 확인
      if (formatted.length === 12 && value.length !== 10) {
        input.setCustomValidity('사업자 번호는 10자리여야 합니다');
      } else {
        input.setCustomValidity('');
      }
    }
    
    // 입력 필드에 이벤트 리스너 추가
    document.addEventListener('DOMContentLoaded', function() {
      // 핸드폰 번호 포맷팅
      const pharmacistPhone = document.getElementById('pharmacistPhone');
      if (pharmacistPhone) {
        pharmacistPhone.addEventListener('input', function() {
          formatPhoneNumber(this);
        });
      }
      
      // 약국 전화번호 포맷팅
      const pharmacyPhone = document.getElementById('pharmacyPhone');
      if (pharmacyPhone) {
        pharmacyPhone.addEventListener('input', function() {
          formatTelNumber(this);
        });
      }
      
      // 사업자 번호 포맷팅
      const businessNumber = document.getElementById('businessNumber');
      if (businessNumber) {
        businessNumber.addEventListener('input', function() {
          formatBusinessNumber(this);
        });
      }
    });
    
    // 폼 제출 처리
    async function handleSubmit(event) {
      event.preventDefault();
      
      // 모든 필수 필드 검증
      const form = document.getElementById('signupForm');
      const inputs = form.querySelectorAll('input[required]');
      let isValid = true;
      let firstErrorField = null;
      
      inputs.forEach(input => {
        input.classList.remove('error');
        
        if (!input.value.trim()) {
          isValid = false;
          input.classList.add('error');
          
          if (!firstErrorField) {
            firstErrorField = input;
          }
        }
      });
      
      if (!isValid) {
        alert('모든 필수 항목을 입력해주세요.');
        if (firstErrorField) {
          firstErrorField.focus();
        }
        return;
      }
      
      // 핸드폰 번호 유효성 검증
      const phonePattern = /^01[0-9]-\d{3,4}-\d{4}$/;
      const pharmacistPhone = document.getElementById('pharmacistPhone').value;
      if (!phonePattern.test(pharmacistPhone)) {
        alert('올바른 핸드폰 번호 형식이 아닙니다 (예: 010-1234-5678)');
        document.getElementById('pharmacistPhone').classList.add('error');
        document.getElementById('pharmacistPhone').focus();
        return;
      }
      
      // 사업자 번호 유효성 검증 (10자리)
      const businessNumber = document.getElementById('businessNumber').value.replace(/[^0-9]/g, '');
      if (businessNumber.length !== 10) {
        alert('사업자 번호는 10자리여야 합니다 (예: 123-45-67890)');
        document.getElementById('businessNumber').classList.add('error');
        document.getElementById('businessNumber').focus();
        return;
      }
      
      // 폼 데이터 수집
      const formData = {
        pharmacistName: document.getElementById('pharmacistName').value,
        pharmacistPhone: document.getElementById('pharmacistPhone').value,
        businessNumber: document.getElementById('businessNumber').value,
        pharmacyName: document.getElementById('pharmacyName').value,
        pharmacyPhone: document.getElementById('pharmacyPhone').value,
        postcode: document.getElementById('postcode').value,
        address: document.getElementById('address').value,
        detailAddress: document.getElementById('detailAddress').value || '',
        referralCode: document.getElementById('referralCode').value.trim().toUpperCase() || null,
        googlePicture: userPicture
      };
      
      console.log('회원가입 데이터:', formData);
      
      // 서버로 데이터 전송
      try {
        const response = await fetch('/api/signup', {
          method: 'POST',
          headers: {
            'Content-Type': 'application/json',
            'Authorization': `Bearer ${accessToken}`
          },
          body: JSON.stringify(formData)
        });

        const result = await response.json();

        if (response.ok && result.success) {
          // 회원가입 성공
          alert('회원가입이 완료되었습니다! 로그인 페이지로 이동합니다.');
          window.location.href = '/login';
        } else {
          // 회원가입 실패
          alert(result.message || '회원가입에 실패했습니다.');
        }
      } catch (error) {
        console.error('회원가입 요청 에러:', error);
        alert('서버 연결에 실패했습니다. 잠시 후 다시 시도해주세요.');
      }
    }
  </script>
</body>
</html>
